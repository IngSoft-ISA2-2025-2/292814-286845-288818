name: Backend Buildname: Backend Build



on:on:

  push:  push:

    branches: [ main, develop, feature/* ]    branches: [ main, develop, feature/* ]

  pull_request:  pull_request:

    branches: [ main, develop ]    branches: [ main, develop ]

  workflow_dispatch:  workflow_dispatch:



env:env:

  WORKING_DIRECTORY: 'Obligatorio/Material/Codigo/Backend'  WORKING_DIRECTORY: 'Obligatorio/Material/Codigo/Backend'



jobs:jobs:

  detect-backend-changes:  detect-backend-changes:

    name: Detect Backend Changes    name: Detect Backend Changes

    runs-on: ubuntu-latest    runs-on: ubuntu-latest

    outputs:    outputs:

      backend-changed: ${{ steps.changes.outputs.backend }}      backend-changed: ${{ steps.changes.outputs.backend }}

    steps:    steps:

    - name: Checkout code    - name: Checkout code

      uses: actions/checkout@v4      uses: actions/checkout@v4

      with:      with:

        fetch-depth: 0        fetch-depth: 0



    - name: Check for backend changes    - name: Check for backend changes

      id: changes      id: changes

      run: |      run: |

        echo "🔍 Checking for backend changes..."        echo "🔍 Checking for backend changes..."

                

        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then

          echo "Manual trigger - running build"          echo "Manual trigger - running build"

          echo "backend=true" >> $GITHUB_OUTPUT          echo "backend=true" >> $GITHUB_OUTPUT

          exit 0          exit 0

        fi        fi

                

        # For pull requests, compare against base branch        # For pull requests, compare against base branch

        if [ "${{ github.event_name }}" = "pull_request" ]; then        if [ "${{ github.event_name }}" = "pull_request" ]; then

          BASE_SHA="${{ github.event.pull_request.base.sha }}"          BASE_SHA="${{ github.event.pull_request.base.sha }}"

          HEAD_SHA="${{ github.sha }}"          HEAD_SHA="${{ github.sha }}"

        else        else

          # For pushes, compare with previous commit          # For pushes, compare with previous commit

          BASE_SHA="${{ github.event.before }}"          BASE_SHA="${{ github.event.before }}"

          HEAD_SHA="${{ github.sha }}"          HEAD_SHA="${{ github.sha }}"

        fi        fi

                

        # Check if backend files changed        # Check if backend files changed

        if git diff --name-only "$BASE_SHA" "$HEAD_SHA" | grep -q "Obligatorio/Material/Codigo/Backend"; then        if git diff --name-only "$BASE_SHA" "$HEAD_SHA" | grep -q "Obligatorio/Material/Codigo/Backend"; then

          echo "✅ Backend changes detected"          echo "✅ Backend changes detected"

          echo "backend=true" >> $GITHUB_OUTPUT          echo "backend=true" >> $GITHUB_OUTPUT

        else        else

          echo "⏭️ No backend changes detected - build will skip"          echo "⏭️ No backend changes detected - build will skip"

          echo "backend=false" >> $GITHUB_OUTPUT          echo "backend=false" >> $GITHUB_OUTPUT

        fi        fi



  build:  build:

    name: Backend Build    name: Backend Build

    runs-on: ubuntu-latest    runs-on: ubuntu-latest

    needs: detect-backend-changes    needs: detect-backend-changes

    if: always()    if: always()

        

    steps:    steps:

    - name: Skip if no backend changes    - name: Skip if no backend changes

      if: needs.detect-backend-changes.outputs.backend-changed != 'true'      if: needs.detect-backend-changes.outputs.backend-changed != 'true'

      run: |      run: |

        echo "⏭️ Skipping backend build - no backend changes detected"        echo "⏭️ Skipping backend build - no backend changes detected"

        echo "✅ This check passes automatically when no backend changes are present"        echo "✅ This check passes automatically when no backend changes are present"

        exit 0        exit 0

            - name: Checkout code

    - name: Checkout code      if: needs.detect-backend-changes.outputs.backend-changed == 'true'

      if: needs.detect-backend-changes.outputs.backend-changed == 'true'      uses: actions/checkout@v4

      uses: actions/checkout@v4

    - name: Setup .NET

    - name: Setup .NET      if: needs.detect-backend-changes.outputs.backend-changed == 'true'

      if: needs.detect-backend-changes.outputs.backend-changed == 'true'      uses: actions/setup-dotnet@v4

      uses: actions/setup-dotnet@v4      with:

      with:        dotnet-version: '6.0.x'

        dotnet-version: '6.0.x'

    - name: Cache NuGet packages

    - name: Cache NuGet packages      if: needs.detect-backend-changes.outputs.backend-changed == 'true'

      if: needs.detect-backend-changes.outputs.backend-changed == 'true'      uses: actions/cache@v4

      uses: actions/cache@v4      with:

      with:        path: ~/.nuget/packages

        path: ~/.nuget/packages        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}

        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}        restore-keys: |

        restore-keys: |          ${{ runner.os }}-nuget-

          ${{ runner.os }}-nuget-

    - name: Restore dependencies

    - name: Restore dependencies      if: needs.detect-backend-changes.outputs.backend-changed == 'true'

      if: needs.detect-backend-changes.outputs.backend-changed == 'true'      working-directory: ${{ env.WORKING_DIRECTORY }}

      working-directory: ${{ env.WORKING_DIRECTORY }}      run: |

      run: |        echo "🔄 Restoring NuGet packages..."

        echo "🔄 Restoring NuGet packages..."        dotnet restore

        dotnet restore        echo "✅ Dependencies restored successfully"

        echo "✅ Dependencies restored successfully"

    - name: Build Application

    - name: Build Application      if: needs.detect-backend-changes.outputs.backend-changed == 'true'

      if: needs.detect-backend-changes.outputs.backend-changed == 'true'      working-directory: ${{ env.WORKING_DIRECTORY }}

      working-directory: ${{ env.WORKING_DIRECTORY }}      run: |

      run: |        echo "🏗️ Building .NET application..."

        echo "🏗️ Building .NET application..."        dotnet build --configuration Release --no-restore --verbosity normal

        dotnet build --configuration Release --no-restore --verbosity normal        echo "✅ Build completed successfully"

        echo "✅ Build completed successfully"

    - name: Upload build artifacts

    - name: Upload build artifacts      if: success() && needs.detect-backend-changes.outputs.backend-changed == 'true'

      if: success() && needs.detect-backend-changes.outputs.backend-changed == 'true'      uses: actions/upload-artifact@v4

      uses: actions/upload-artifact@v4      with:

      with:        name: backend-build-artifacts

        name: backend-build-artifacts        path: |

        path: |          ${{ env.WORKING_DIRECTORY }}/**/bin/Release/

          ${{ env.WORKING_DIRECTORY }}/**/bin/Release/          ${{ env.WORKING_DIRECTORY }}/**/obj/Release/

          ${{ env.WORKING_DIRECTORY }}/**/obj/Release/        retention-days: 1

        retention-days: 1

    - name: Build Summary

    - name: Build Summary      if: always()

      if: always()      run: |

      run: |        if [ "${{ needs.detect-backend-changes.outputs.backend-changed }}" != "true" ]; then

        echo "## 🏗️ Backend Build Results" >> $GITHUB_STEP_SUMMARY          echo "## ⏭️ Backend Build - Skipped" >> $GITHUB_STEP_SUMMARY

        if [ "${{ needs.detect-backend-changes.outputs.backend-changed }}" = "true" ]; then          echo "No backend changes detected in this commit/PR" >> $GITHUB_STEP_SUMMARY

          if [[ "${{ job.status }}" == "success" ]]; then          echo "✅ **Status**: Passed (no build needed)" >> $GITHUB_STEP_SUMMARY

            echo "✅ **Build Status**: Successful" >> $GITHUB_STEP_SUMMARY        else

          else          echo "## 🏗️ Backend Build Results" >> $GITHUB_STEP_SUMMARY

            echo "❌ **Build Status**: Failed" >> $GITHUB_STEP_SUMMARY          echo "✅ **Build Status**: Successful" >> $GITHUB_STEP_SUMMARY

          fi          echo "- Configuration: Release" >> $GITHUB_STEP_SUMMARY

          echo "- Configuration: Release" >> $GITHUB_STEP_SUMMARY          echo "- .NET Version: 6.0" >> $GITHUB_STEP_SUMMARY

          echo "- .NET Version: 6.0" >> $GITHUB_STEP_SUMMARY          echo "- Working Directory: ${{ env.WORKING_DIRECTORY }}" >> $GITHUB_STEP_SUMMARY

          echo "- Working Directory: ${{ env.WORKING_DIRECTORY }}" >> $GITHUB_STEP_SUMMARY        fi
        else
          echo "⏭️ **Build Status**: Skipped (no backend changes detected)" >> $GITHUB_STEP_SUMMARY
          echo "- This check passes automatically when no backend changes are present" >> $GITHUB_STEP_SUMMARY
        fi